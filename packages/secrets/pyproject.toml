[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/redteam_secrets"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""

[project]
name = "red-teaming-secrets"
version = "0.1.0"
description = "Secret resolution by reference: environment variables, or one file per secret."
requires-python = ">=3.12,<3.14"
dependencies = []
