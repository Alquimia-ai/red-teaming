[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/redteam_delivery"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""

[project]
name = "red-teaming-delivery"
version = "0.1.0"
description = "Delivering a run's outcome to the consumer's webhook, and saying whether it landed."
requires-python = ">=3.12,<3.14"
dependencies = [
    "httpx>=0.28",
]
