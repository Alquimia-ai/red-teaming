[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/redteam_contracts"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""

[project]
name = "red-teaming-contracts"
version = "0.1.0"
description = "Domain types: run spec, plan, trace, manifest, contract, knowledge base. Zero IO."
requires-python = ">=3.12,<3.14"
dependencies = [
    "pydantic>=2.9",
    "pyyaml>=6.0",
]
